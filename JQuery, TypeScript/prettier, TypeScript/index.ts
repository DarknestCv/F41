// ! npm i -D -E prettier
// -D - скорочення від -save-dev
// -E - скорочення від -save-exe це команда для фіксації версії
// prettier - потрібна качати з фіксована версія

// ! в корені потрібно свторбвати prettierrc, prettierignore
// ? "prettier:check": "prettier . --check" - перевірка файлів
// ? "prettier:write": "prettier . --write" - перезапис файлів


let a = {x: 10, y:'1', z:"2"}

const s = new Set()


// ctrl+c * 2 - вбиває процес


import "./button"


// ! для того щоб сказати що там точно буде якесь значення а не null використовуємо оператор (!)
//  приклад const df = getElementsByTagName('button')!

