{
    "compilerOptions": {
        "allowJs": true, // потрібно для того щоб якщо навіть старіший джс то всерівно компелював в потрібну  нам екму
        "noEmit": true,
        "strict": true, // строга типізація
        "target": "ES5",
        "lib": ["ES5","ES2015.Generator"], // так можна додати інфу додаткову з новішой екми(js), можна підеключати окремі типи даних через (.)
        "outFile": "./bundle/bundle.js", // в такому випадку буде рекурсія 
        "exclude": ["./bundle"],
    }
}